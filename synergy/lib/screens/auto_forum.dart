import 'package:flutter/material.dart';

class AutoForumScreen extends StatelessWidget {
  const AutoForumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Auto Forum/Community'),
      ),
      body: const Center(
        child: Text('Auto Forum/Community Screen'),
      ),
    );
  }
}